/** The routine icon library's keys (`RoutineIcon`'s known `iconKey`s, minus the `dressed` alias), used by
 *  `IconPicker` for routine steps (Task 6) and sticker categories. */
export const ICON_KEYS = [
  'breakfast', 'teeth', 'getting-dressed', 'shoes', 'car', 'books', 'snack', 'park', 'nap', 'potty', 'bath', 'bed',
] as const
